package main

var arr = [1, 2, 3] i32;

var i = 0;
while (i < len(arr)) {
    var curr = arr[i];
    print(curr);
    
    i++;
}

for (var j = 0; j < len(arr); j++) {
    var curr = arr[j];
    print(curr);
}

foreach (var k in arr) {
    print(k);
}