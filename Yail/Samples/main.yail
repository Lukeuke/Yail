using math

package codewars

pub funky solve(arr []i32) i32 {
    var output = 0;
    
    foreach (var x in arr) {
        output = output + math::pow(x, 2);
    }
    
    return output;
}

package main

var arr = [1, 2, 2] i32;
println(codewars::solve(arr));